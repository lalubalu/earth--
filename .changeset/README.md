# Changesets

Run `pnpm changeset` after changing `packages/signal-engine`, pick a bump, describe the change. `pnpm version-packages` applies pending changesets; the release workflow publishes when `NPM_TOKEN` is configured.
