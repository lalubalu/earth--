## Summary

## Checklist

- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build` pass locally
- [ ] New `.ts/.tsx/.js/.mjs/.css/.glsl` files carry the programmer header
- [ ] Engine changes include tests and a changeset (`pnpm changeset`)
- [ ] No fabricated data, screenshots, or stats
